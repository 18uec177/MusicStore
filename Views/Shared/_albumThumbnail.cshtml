@model AlbumModel


<div class="card mb-4 shadow-sm">
    <img src="/images/grey background.jpg" class="img-fluid shadow rounded" />
    <div class="card-body">
        <h3 class="card-text">@(string.IsNullOrEmpty(Model.Title) ? "Name is not available" : Model.Title)</h3>

    </div>
</div>


