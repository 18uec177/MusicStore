@{ViewBag.Title = "Albums";}

<div>

    @model IEnumerable<AlbumModel>

    <div class="container ">
        <h3 style="font-style:italic">Album according to Genre</h3>
        <div class="row">


             
            @foreach (var album in Model)
            {
<div class="col-md-4  ">
    <partial name="_albumList" model="@album" />

</div>            }


        </div>
    </div>
</div>